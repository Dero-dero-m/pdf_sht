# Copy to infra/.env (gitignored) and edit values.
# These are read by docker-compose.yml via ${VAR:-default} interpolation;
# Compose loads this file automatically when it sits next to docker-compose.yml.

POSTGRES_USER=postgres
POSTGRES_PASSWORD=changeme
POSTGRES_DB=app
